# Ralph Progress Log
Started: Thu Mar 12 00:46:01 CST 2026
## Codebase Patterns
- Any asset referenced from `README.md` should live in a committed docs path such as `docs/images/`, not under runtime-oriented `storage/`.
- Keep `scripts/ralph/prd.json` stories as `passes: false` on the feature branch until the implementation has been merged into `main` and `main` has been pushed; flip the flag in a follow-up commit on `main`.
- If `storage/` needs to keep a canonical checked-in dataset, ignore `storage/*` and explicitly unignore only the approved files so runtime noise stays local.
- In this shell, networked Node tests need `NODE_USE_ENV_PROXY=1` so `fetch` honors the configured `HTTP_PROXY`/`HTTPS_PROXY` variables during Amadeus integration checks.
---
## 2026-03-12 00:55:18 CST - US-001
- What was implemented: Copied the latest local UI screenshot from `storage/ctrip/screenshots/PVG-URC-2026-02-26.png` into `docs/images/pvg-urc-2026-02-26.png` and embedded it in `README.md` with a relative Markdown image path.
- Files changed: `README.md`, `docs/images/pvg-urc-2026-02-26.png`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- Quality checks run: `pnpm test`; `git diff --check`; verified `docs/images/pvg-urc-2026-02-26.png` exists and `README.md` references it with a relative path.
- Commit created: `feat: US-001 - 在 README 中嵌入最新的 UI 截图`
- Learnings for future iterations:
  - Patterns discovered: README-facing assets should be copied into a committed docs folder before any `storage/` cleanup.
  - Gotchas encountered: `scripts/ralph/` is currently untracked in this worktree, so stage only the specific Ralph state files you intend to commit.
  - Useful context: No nearby repo `AGENTS.md` files were present to update. Browser verification was not applied to README rendering; a manual GitHub/local Markdown preview is still worth checking after merge.
---
## 2026-03-12 01:14:34 CST - US-001
- What was implemented: Replayed the Ralph completion flow for US-001 by keeping the feature branch in a pre-pass state, merging `ralph/storage-readme-cleanup` into `main`, pushing `main` to `origin`, and then marking the story complete in `prd.json`.
- Files changed: `README.md`, `docs/images/pvg-urc-2026-02-26.png`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- Quality checks run: `pnpm test`; `git diff --check`; `git merge --no-ff main` on the feature branch reported already up to date; merged the feature branch into `main`; pushed `main` to `origin`.
- Commit created: `chore: mark US-001 complete after main push`
- Learnings for future iterations:
  - Patterns discovered: When a Ralph story is already implemented on the feature branch, a small follow-up commit on `main` is enough to record completion after the required merge/push sequence.
  - Gotchas encountered: `README.md` rendering was not browser-verified in this environment; manual Markdown/GitHub preview is still required for visual confirmation.
  - Useful context: No nearby repo `AGENTS.md` files were present to update during this iteration.
---
## 2026-03-12 01:18:38 CST - US-002
- What was implemented: Replaced the blanket `storage/` ignore with an allowlist for the canonical 330-day Ctrip CSV artifacts, removed the other local `storage/` runtime outputs, merged the resulting change through `ralph/storage-readme-cleanup` into `main`, and pushed `main` to `origin`.
- Files changed: `.gitignore`, `storage/ctrip_prices_PVG_URC_330d.csv`, `storage/ctrip_quality_PVG_URC_330d.csv`, `storage/ctrip_quality_PVG_URC_330d_suspicious.csv`, `storage/ctrip_spotcheck_PVG_URC_330d.csv`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- Quality checks run: `find storage -maxdepth 1 -type f | sort`; `git check-ignore -v` for kept vs removed storage files; `pnpm test`; `git diff --check`; `git fetch origin main`; `git merge --no-ff origin/main` on the feature branch; `git merge --no-ff ralph/storage-readme-cleanup` on `main`; `git push origin main`.
- Commit created: `chore: mark US-002 complete after main push`
- Learnings for future iterations:
  - Patterns discovered: Use `.gitignore` allowlists for canonical sample data in otherwise runtime-oriented directories.
  - Gotchas encountered: A blanket ignored `storage/` directory hides cleanup progress from git, so verify the intended tracked set with `find storage` and `git check-ignore -v`.
  - Useful context: No nearby repo `AGENTS.md` files were present to update during this iteration.
---
## 2026-03-12 01:24:05 CST - US-003
- What was implemented: Replaced the absolute filesystem doc references in `README.md` with relative Markdown links to `docs/PRODUCT.md` and `docs/DEVELOPMENT.md`, merged the change through `ralph/storage-readme-cleanup` into `main`, and pushed `main` to `origin`.
- Files changed: `README.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- Quality checks run: `rg -n '/Users/wenfeng/Documents/code/travel-deal-planner/docs/|\\[docs/' README.md`; `pnpm test`; `git diff --check`; `git fetch origin main`; `git merge --no-ff origin/main` on the feature branch; `pnpm test` on `main` failed twice with Amadeus connect timeouts; `NODE_USE_ENV_PROXY=1 pnpm test`; `git merge --no-ff ralph/storage-readme-cleanup` on `main`; `git push origin main`.
- Commit created: `chore: mark US-003 complete after main push`
- Learnings for future iterations:
  - Patterns discovered: For README docs sections, prefer relative Markdown links over raw filesystem paths so both local preview and GitHub rendering stay clickable.
  - Gotchas encountered: The Amadeus integration tests can time out in this environment unless Node proxy support is enabled, even when `curl` can reach the host via `HTTP_PROXY`/`HTTPS_PROXY`.
  - Useful context: No nearby repo `AGENTS.md` files were present to update during this iteration. README link rendering was not browser-verified; a manual Markdown/GitHub preview is still worth checking.
---
